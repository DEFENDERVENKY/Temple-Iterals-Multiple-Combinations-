
 # Conditional logic inside a function
 # This example uses a function that checks if a number is even or odd and incorporates that result into the template literal.

javascript


function isEven(num) {
    return num % 2 === 0 ? 'even' : 'odd';
}

let number = 10;
console.log(`The number ${number} is ${isEven(number)}.`);

// Output //

The number 10 is even.

